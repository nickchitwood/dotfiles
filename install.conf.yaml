- defaults:
    link:
      relink: true
- clean: ['~']

- link:
    ~/.gtkrc-2.0:
    ~/.zshenv:
    ~/.zshrc:
    ~/.zprofile:
    ~/.aliases:
    ~/.p10k.zsh:
    ~/.gitconfig:
    ~/.config/fontconfig:    
    ~/.config/glances:
    ~/.config/i3:
    ~/.config/gtk-3.0:
    ~/.config/gtk-4.0:
    ~/.config/i3status:
    ~/.config/kanshi:
    ~/.config/kitty:
    ~/.config/mimeapps.list:
    ~/.config/picom:
    ~/.config/quodlibet:
    ~/.config/ranger:
    ~/.config/rofi:
    ~/.config/mpv:
    ~/.config/pacman: 
    ~/.config/nvim: 
    ~/.config/paru: 
    ~/.config/sway:
    ~/.config/waybar:
    ~/.config/wayvnc:
    ~/.config/wofi:
    ~/.ssh/config: ssh/config
    ~/.tmux.conf:

- create:
    - ~/.config
    - ~/.cache
      
- shell:
    - [git submodule update --init --recursive, Installing submodules]
