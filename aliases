# Alias lookup

# Developer
alias v='nvim'
alias vim='nvim'
alias sudo='sudo '

# All the color
alias ip='ip -color=auto'

# Website apps
alias asana='google-chrome-stable --app=https://asana.com'

# RDP
rdpwork() {
	xfreerdp /u:nchitwood /d:rcoe.us /size:2560x1440 /rfx /v:$1
}
rdphome() {
	xfreerdp /u:nicholaschitwood@gmail.com /size:2560x1440 /rfx /v:$1
}
